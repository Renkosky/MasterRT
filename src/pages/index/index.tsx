/**
 *
 * Routes:
 *  - src/theme/route-guard.tsx
 *
 *
 */
import $theme from 'src/theme';
import dynamic from 'umi/dynamic';
import * as React from 'react';

const module = $theme.getModule('index');
const component = module.component;

export default component;

