import * as React from 'react';
import * as mu from 'mzmu';
import * as _ from 'lodash';

export default class NodataComponent extends React.Component<{}, {}> {
    render() {
        console.debug('--------');
        return (<div> nodata test demo</div>);
    }
}